---
import UtterancesComment from "@components/comment/Utterances.astro";
import GitalkComment from "@components/comment/Gitalk";

export interface Props {
  slug: string;
}

const { slug } = Astro.props;
---

<section class="mt-16 p-0">
  <!-- <h2 class="inline-block text-xl font-medium text-skin-base">Comments</h2> -->
  <UtterancesComment slug={slug} />
  <!-- <GitalkComment client:only slug={slug} /> -->
</section>
